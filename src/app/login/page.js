import Auth from "@/components/dashboard/Auth";

export const metadata = {
  title: "Admin Login | Vijay Thakur",
  description: "Login to access the dashboard",
};

export default function LoginPage() {
  return <Auth />;
}
